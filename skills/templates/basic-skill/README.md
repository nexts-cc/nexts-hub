# Basic Skill Template

Copy this folder to `skills/<skill-name>` and replace the frontmatter in `SKILL.md`.

Required:

- `SKILL.md` with `name` and `description` frontmatter.

Recommended:

- `agents/nextsai.yaml` for UI metadata.
- `references/` for optional documentation.
- `scripts/` for deterministic helper code.
