Runtime site-pattern files live in `runtime/site-patterns/`.

Agent workflows should check and update `runtime/site-patterns/{domain}.md` for reusable selectors, waits, navigation notes, CSP needs, and extraction pitfalls. The native host copies this runtime directory during macOS installation.
